const product =  (a, b) => a + b;

const divide = (a, b) => { 
    if (b != 0) return a / b;
    return null;
}

const power = (a, b) => a ** b;

const subtract = (a, b) => a - b;

const add = (x, y) => x + y;
